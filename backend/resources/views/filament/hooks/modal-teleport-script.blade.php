{{-- Intentionally empty. Modal positioning is handled in CSS via the
     `html:has(.fi-modal-open)` rules in admin-theme.css that strip
     backdrop-filter from ancestors while a modal is open, freeing the
     modal's `position: fixed` from any containing block created by
     this theme's frosted-glass surfaces. --}}
