{{-- Tiny version stamp at the bottom of the sidebar. Source of truth
     lives in config/app.php under the `version` key (bumped before
     every deploy). --}}
<div class="evrst-sidebar-version" aria-label="App version">
    v{{ config('app.version') }}
</div>
